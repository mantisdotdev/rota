# Acceptance scenarios

Every behaviour in the PRD has one row here, and every engine test names the ID it covers (PRD §12 rule 2). T-01–T-24 are PRD §13 unchanged. T-25 onward were added on 2026-09-02 for §6 behaviours that had no scenario, T-85 onward on 2026-09-03 for the §9 views and the lights, T-95 and T-96 the same day for the last two §8.2 gestures; the PRD section each comes from is in brackets.

Conventions: fractions are of one cycle; the default kit (lofi), C minor and 100 bpm are assumed unless stated; "tap ×n" means n taps on that pad starting from empty, following the kit's smart defaults (§6.6); per-track modifier strings are the share-code form from `spec/share-format.md` §3. IDs are never reused: retire a scenario by striking it through, not by deleting it.

| ID | Scenario | Expected |
|---|---|---|
| T-01 | Tap kick once | One event at 0. |
| T-02 | Tap kick three times | Events at 0, 1/3, 2/3. Ring shows three dividers. |
| T-03 | Tap snare once (default kit) | Steps `~ sd`; one event at 1/2. |
| T-04 | Tap snare twice | Steps `~ sd ~ sd`; events at 1/4 and 3/4. |
| T-05 | Kick ×4, snare ×2, hat ×2 | Classic beat: kick 0,1/4,1/2,3/4; snare 1/4,3/4; hat 0,1/4,1/2,3/4 (templates: `hh hh` then `hh hh hh hh`). |
| T-06 | Hat ×1 (template `hh hh`) then split on hat | Steps `hh hh*2`; events at 0, 1/2, 3/4. |
| T-07 | Split pressed with no pad tap for 5 s | Disarms; status "add a hit first" only if a pad with no steps is tapped. |
| T-08 | Swap on clap (`a`), swap again (`b`) | `a` plays on cycles 0,2,4…; `b` on 1,3,5…; `fourth` on 3,7,11… |
| T-09 | Hold hat + speed knob one detent up | Hat track `speed = 2`; a two-step hat plays at 0,1/4,1/2,3/4. |
| T-10 | Chance at 0 after chance at 1 | Event list identical to the authored pattern. |
| T-11 | Chance 1, seed 42, 100 cycles | Drop rate 60% ± 5 on drums; ghost rate 30% ± 5; chords never dropped below p 0.5. |
| T-12 | Every pad ×4, all split ×4, chance 1, fx 1 | Firmware keeps 16 voices without dropouts at 48 kHz; CPU < 80%. |
| T-13 | Undo after any edit | Exact previous state, including templates. 60 levels. |
| T-14 | Skip on kick with `bd bd` | `bd bd ~`; events at 0, 1/3. |
| T-15 | Encode → decode → encode any state | Identical string. Includes all golden codes. |
| T-16 | Load a code with an unknown future field | Loads; unknown field ignored; no error. |
| T-17 | Switch to empty section B | B equals A. Switch lands at the next cycle boundary. |
| T-18 | Edit at cycle fraction 0.30 | Takes effect at 0.50 (next beat), audition sound plays at 0.30. |
| T-19 | Two devices linked, second presses play | Second device follows first's clock; playhead phase difference < 3 ms. |
| T-20 | Unplug jam cable mid-loop | Both keep playing, no glitch, each on its own clock. |
| T-21 | Headphones inserted | Speaker mutes within 50 ms; headphone path full range. |
| T-22 | First-boot tutorial | Completes in ≤ 45 s for a tester following prompts; skippable with play. |
| T-23 | Battery: headphones, 100 bpm, default kit, screen on | ≥ 20 h. |
| T-24 | Cold boot to playable | ≤ 1 s. |
| T-25 | Hold the snare pad (`~ sd ~ sd`) and press volume down twice (§6.2, §8.1, D-012) | Snare level 0.8 → 0.6 in state; the snare is audibly quieter; master volume unchanged; the share code carries it as the snare's per-track modifiers `,6a1a` and reloads to 0.6. |
| T-26 | Hold the hat pad and turn the filter knob one detent down (§6.2, §8.1) | Hat tone 1.0 → 0.9; global filter and every other track unchanged; hat modifiers `,891a`. |
| T-27 | Hold the clap pad and turn the fx knob one detent up (§6.2, §8.1) | Clap send 0.1 → 0.2; global fx unchanged; clap modifiers `,8a2a`. |
| T-28 | Global chance 1; hold the kick pad and turn the chance knob one detent down (§6.2, §6.5) | Kick track chance 0.9, so `p = 0.9` on the kick and `1.0` on every other track; kick modifiers `,8a19`. |
| T-29 | Hat `hh hh hh hh`, then hold hat + speed knob one detent down (§6.2) | Hat `speed = 0.5`: cycle 0 plays steps 0–1 at 0 and 1/2; cycle 1 plays steps 2–3 at 0 and 1/2; cycle 2 repeats cycle 0. Track string `eh0000`. |
| T-30 | Rim ×1, then swap three times (every → a → b → fourth) (§6.2) | Rim plays only on cycles 3, 7, 11, … (cycle index mod 4 == 3); the ring draws it at 30% opacity on the other cycles; track string `f10`. |
| T-31 | Chord ×4 (`Cm Ab Eb Bb`), then bass ×2 (§6.4, §8.4) | Bass events at 0 and 1/2; the first sounds C (root of Cm, the chord active at 0), the second Eb (root of Eb, active at 1/2). With the chord track emptied, both bass events sound the key root C. |
| T-32 | Hat ×1, ×2, ×3 (§6.6) | Tap 1: `hh hh`, events 0, 1/2. Tap 2: `hh hh hh hh`, events 0, 1/4, 1/2, 3/4. Tap 3 appends: five hits at 0, 1/5, 2/5, 3/5, 4/5. |
| T-33 | Clap ×1, ×2, ×3 (§6.6) | Tap 1: `~ cp`, event 1/2. Tap 2: `~ cp ~ cp`, events 1/4, 3/4. Tap 3 appends: `~ cp ~ cp cp`, events 1/5, 3/5, 4/5. |
| T-34 | 61 edits in section A, then undo 61 times (§6.7, §8.2) | The first 60 undos each restore the exact previous state, ending at the state after edit 1; the 61st undo changes nothing. Hold undo redoes edit 2. Section B's undo history is untouched throughout. |
| T-35 | Hold the kick pad for two cycles, then release (§6.2 `mute`) | No kick events while held; other tracks unaffected; on release the kick pattern returns unchanged; the share code never shows the mute. |
| T-36 | Any pattern, chance 0, seed 42 (§6.4) | Every first sub-hit has velocity in 0.9–1.0 after ±5% humanize; sub-hits after the first are ×0.8; ghosts (when chance > 0) are ×0.55; the same seed gives the same velocities on every run. |
| T-37 | Hat ×6 (eight steps, one per eighth), swing 0.15 (§6.3) | Events on odd eighths (1/8, 3/8, 5/8, 7/8) are delayed by 0.15 × 1/24 = 1/160 cycle (15 ms at 100 bpm); events on even eighths (0, 1/4, 1/2, 3/4) do not move; with swing 0 nothing moves. |
| T-38 | Kick with 16 steps, tap kick again (§6.1, D-013, D-024) | The tap adds nothing: still 16 steps, events unchanged; the audition sound still plays; the status line reads `kick is full`. |
| T-39 | Song with arrangement `AABABBCD` in song play mode (§6.8, D-025) | Sections play in the order A A B A B B C D, one cycle each, then loop; each change lands on a cycle boundary. |
| T-40 | Sections A and B both non-empty; switch A → B (§6.8) | B plays as it was; nothing is copied from A; the switch lands at the next cycle boundary (as T-17). |
| T-41 | Chord ×5 (§8.4, §6.4) | Steps get i, VI, III, VII, i: `Cm Ab Eb Bb Cm`; note indices 0, 1, 2, 3, 0 in the share code. The engine names the position a sixth tap would take, 1, which is what the pad auditions before the beat; a drum pad's is always 0 (D-036, D-085). |
| T-42 | Kick `bd bd`, chance 1, seed 42, 100 cycles (§6.5) | Ghosts land only at the step midpoints 1/4 and 3/4, at velocity ×0.55; the bass, chord and pluck tracks gain no ghosts in any cycle. |
| T-43 | Chord `Cm Ab Eb Bb`; change key from C minor to A minor in settings (§6.3, §8.4) | The chord track plays `Am F C G`; the bass follows the new roots; the share code's note indices stay `0123`; only the key field changes from `cm` to `am`. |
| T-44 | Pluck ×9 in C minor (§8.4, Appendix A, D-020) | Steps take lofi's pluck sequence in order — degrees `0 2 4 7 9 7 4 2`, sounding C Eb G C′ Eb′ C′ G Eb — and the ninth wraps to position 0 (C). Every note is in the scale. The engine names the tenth tap's position, 1, and 0 for an empty pluck track (D-036, D-085). |
| T-45 | Encode any state, and every golden code (§10.1, D-018) | The string contains only `A–Z a–z 0–9 : . , - ~` (plus `;` and `/` in a song code); placed after `#` in a URL it needs no percent-encoding and `location.hash` returns it unchanged. |
| T-46 | Open the player from a `file://` URL with the network off, G-04 after the `#` (§9.5, §10.3, D-019) | The beat plays on the first pad tap; the page makes no external request; the player is one HTML file of at most 4 MB. |
| T-47 | Load a code with chord note `5` on lofi, whose progression has four entries (§8.4, D-020) | That step plays position 5 mod 4 = 1 (VI, Ab in C minor); the code re-encodes with `5` unchanged. |
| T-48 | Chord ×4 with root C in each of the five modes (Appendix B, D-021) | Chord roots: minor C Ab Eb Bb; major C G A F; dorian C F Bb F; pentatonic minor C Bb Eb F; pentatonic major C A D G. Every note of every chord is in the current scale; the bass plays the root degree. |
| T-49 | Hat `hh hh hh` at speed 0.5 (§6.2, D-022) | Cycle 0 plays events at 0 and 2/3; cycle 1 plays one event at 1/3; cycle 2 repeats cycle 0. |
| T-50 | Snare `~ sd`, chance 1, seed 42, 100 cycles (§6.5, D-023) | Ghosts land only at 1/4 (midpoint of the rest step) and 3/4 (midpoint of the hit step), at ×0.55 velocity. |
| T-51 | Encode a song: four full sections, arrangement `AABABBCD`, a lineage (§10.1, D-025; G-15) | Decode → encode reproduces it byte-for-byte. A song with 65 letters, a letter outside A–D, or sections naming different kits does not load; status `that code did not load`. |
| T-52 | Load `RT2:jazz:…` on a device or player that has only lofi (D-026) | The loop loads and plays with lofi; status `no kit jazz, using lofi`; omitted modifier groups take lofi's defaults; encoding now gives `RT2:lofi:…`. |
| T-53 | Kick ×4 and snare ×2 set, other tracks empty; press dice, then hold dice (§8.2, D-028) | Press: the six empty tracks are filled from one of lofi's three starting loops, chosen with the seeded PRNG; kick and snare untouched. Hold: all eight tracks are replaced from a fresh pick; undo restores the previous state. |
| T-54 | From the ring view press show twice, then A, A, B, A; then undo; then hold dice (§9.6, D-030) | The second show press opens the song view; the presses build the arrangement `AABA` without switching sections; undo leaves `AAB`; hold dice empties it and undo restores `AAB`. A 65th letter is refused and the status line reads `song is full`. |
| T-55 | Arrangement `AAB`; from the ring view hold A and press play; later press B (§6.8, D-030) | Song play starts from the top: A, A, B, one cycle each, looping, every change on a cycle boundary; the song view highlights the playing letter. Pressing B leaves song play and switches to section B at the next cycle. With an empty arrangement the gesture starts nothing and the status line reads `song is empty`. |
| T-56 | In the song view tap pad 2 (empty), edit section B, tap pad 1, then power off and on (§6.8, D-030) | Song 2 becomes a copy of song 1 (four sections and arrangement) on the first tap; the edit lands in song 2 only; song 1 plays back unchanged; nothing was saved by hand and the state survives the power cycle. |
| T-57 | Text view after chord ×4 in C minor and in C pentatonic minor (§9.2, D-031) | C minor shows `chord  Cm Ab Eb Bb`; C pentatonic minor shows `chord  C Bb Eb F`: a name is the root plus `m` for a minor triad and the root alone for any other chord. |
| T-58 | Text view after chord ×4 in keys `cm`, `em`, `csm`, `dsm`, `gsdor`, and pluck ×3 in `cm` and `gsdor` (§9.2, D-032) | `cm`: `Cm Ab Eb Bb`; `em`: `Em C G D`; `csm`: `C#m A E B` (C# minor, 4 sharps, beats Db minor, 8 flats); `dsm`: `Ebm Cb Gb Db` (6 sharps against 6 flats, tie → flats); `gsdor`: `Abm Db Gb Db` (Ab dorian, tie → flats); pluck in `cm`: `c5 eb5 g5`; pluck in `gsdor`: `ab5 cb6 eb6` (the octave belongs to the letter, as in scientific pitch notation: MIDI 83 spelled Cb is `cb6`, not `cb5`). The share code spells every one of these roots with `s`. |
| T-59 | Share a loop that was loaded from a code carrying id `k9z2ab`, then load the shared code on a second device (§9.3, §10.2) | Open, io/ scope, session 7: see Open points. The engine encodes whatever lineage the state holds. |
| T-60 | Load `RT2:lofi:096:010:02:00:015:cm:e10-e1-e1-e1-e1-e1-e1-e1`, whose numbers carry leading zeros (share-format §4, D-039) | Loads: bpm 96, filter 1.0, fx 0.2, chance 0, swing 0.15, kick at 0. Re-encodes canonically as `RT2:lofi:96:10:2:0:15:cm:e10-e1-e1-e1-e1-e1-e1-e1`; only a canonical code is promised a byte-identical round trip. |
| T-61 | Generate the lofi kit header from `spec/kits/lofi/kit.json` (§12 rule 6, Appendix A, D-027) | The header carries Appendix A exactly: pad names and sends (drums 0.1, bass 0, chord 0.4, pluck 0.3), synth octaves 2, 4 and 5, the §6.6 tap templates, the five progressions, the pluck sequence, three dice loops that each decode, swing 0.15, filter 1.0, fx 0.2, sidechain on at 5 dB with a 120 ms release. CI regenerates the header and fails on any difference. |
| T-62 | Receive a share code longer than any valid one over SysEx, as USB text, or in the player's paste box (§7.6, §9.5, §10.2, §11) | Open, io/ scope, session 7: see Open points. The engine's decoder is linear in its input and reads a NUL-terminated string; the cap lives where the bytes arrive, before decode sees them. |
| T-63 | A sine at +6 dBFS through the limiter; every golden code rendered for two cycles, G-14 for four (§12 sound spec, D-080) | The output never exceeds −1 dBFS; the gain moves smoothly so the sine keeps its shape; a signal under the ceiling passes unchanged, one millisecond later (the lookahead). Every golden code's file peaks at or under −1 dBFS, and G-14 drives the limiter rather than merely staying quiet. |
| T-64 | A steady tone on the chord pad and a kick event one second in, fx 0; the same tone on the rim pad; the kit's sidechain turned off (§12 sound spec, Appendix A, D-078) | The melodic bus drops 5 dB at the kick's sample and climbs back linearly in dB over 120 ms (−2.5 dB at 60 ms, 0 dB at 120 ms), so the first 10 ms after the kick average −4.8 dB against the 10 ms before; the rim, a drum, is not ducked; with the sidechain off nothing ducks. |
| T-65 | The delay at 100, 120, 180 and 60 bpm; an impulse into it at 100 bpm; a sweep of one bpm per block from 100 to 139 bpm under a 100 Hz sine; set_tempo at 0, at 1000, at a NaN and at ±∞ bpm (§6.3, §12, D-079) | One dotted eighth, 45 / bpm seconds: 21600, 18000, 12000 and 36000 frames at 48 kHz; the impulse comes back exactly 21600 frames later on both channels; the sweep is a chain of complete 10 ms crossfades, each starting only when the last has ended, so the output never moves more than 0.1 per sample and the delay ends heading to 15540 frames; bpm outside 60–180 is clamped to it, infinities included (0 → 36000, 1000 → 12000, +∞ → 12000, −∞ → 36000), and a NaN bpm changes nothing. |
| T-66 | Render T-05 (G-04) twice, lofi dice loop 1 twice, and T-05 at chance 1 twice with seed 42 and once with seed 43 (§12, D-034, D-082) | The two renders of each code are bit-identical, chance 1 included; seed 43 gives a different file. |
| T-67 | A sample voice on a 480-frame ramp: pitch 0 and +12, start 0 and 0.5, decay 1 and 0.5, a release after 100 frames, and a gain of 0.25 (Appendix A, D-075) | Pitch 0 plays the frames as they are, then silence; +12 reads every other frame; start 0.5 begins half way in; decay 0.5 plays half and fades the last 5 ms of it; the release fades the voice out within 5 ms; the output scales by the gain, which the engine sets to the velocity squared. |
| T-68 | The lofi kit's synth presets, and voices at MIDI 36 (sub-saw), 72 (keys) and the C minor triad 60 63 67 (warm-poly) (§12 sound spec, Appendix A, D-076) | sub-saw, warm-poly and keys resolve and an unknown name does not; sub-saw at 36 is loudest at C2 (65.4 Hz), keys at 72 at C5 (523.3 Hz); warm-poly sounds C4, Eb4 and G4 and nothing at C#4 or F4; a keys voice ends on its own. |
| T-69 | A steady 1 kHz tone on the chord pad at level 0.8 then 0.4, tone 1 then 0; a 100 ms tone with send 1 and fx 1, send 1 and fx 0, send 0 and fx 1 (§6.2, D-077) | Level 0.4 gives half the output of 0.8 (linear); tone 0 is a 220 Hz low-pass, so 1 kHz loses 26 dB; send 1 with fx 1 returns an echo one dotted eighth later; fx 0 or send 0 returns nothing. Defaults from the kit: filter 1, fx 0.2, chord send 0.4, level 0.8. |
| T-70 | A steady 1 kHz tone on the rim pad with the filter knob at 1 then 0 (§6.3, D-077) | The knob is exponential: 220 Hz at 0, 20 kHz at 1, their geometric mean at 0.5; at 0 the tone loses 26 dB against 1. |
| T-71 | A 0 dBFS sine, then one at −26 dBFS, through the glue compressor (§12 sound spec, D-080) | 12 dB over the −12 dBFS threshold at 4:1 leaves 9 dB of reduction (within 1.5 dB, with the 3 ms attack and 150 ms release) plus the 3 dB makeup; the quiet sine gets the makeup alone and no reduction. |
| T-72 | The soft clipper on values from −3 to +3 (§12 sound spec, D-080) | Unchanged up to ±0.5 (−6 dBFS), continuous at the knee, monotonic, never beyond ±1. |
| T-73 | A kick, a chord and a pluck at fx 1, then ten seconds of silence; an impulse into a filter, the delay, the reverb and the compressor, then silence (§12 rule 4) | No output sample is ever a subnormal float; the engine's last second is under −120 dBFS with no voice active; the filter's state flushes to exactly zero. |
| T-74 | Sixteen pluck hits in one block, then a seventeenth; two bass notes two blocks apart (§7.5, D-074, D-075) | All sixteen voices sound; the seventeenth steals the voice with the least left to hear, so the count stays at sixteen; the second bass note releases the first, leaving one bass voice a second later. |
| T-75 | `./build/render "<code>" <cycles> out/<name>.wav` with G-04 for 2 cycles, G-11 and G-13 for 1, a song `A;B;C;D/AB` (A one kick, the rest empty) for 4; then a truncated code, `hello`, a count of 0 or 257, and a code naming kit `jazz` (§12, §14 phase 0, D-082) | Exactly cycles × 240 / bpm seconds of 16-bit 48 kHz stereo: 230400, 120000 and 64000 frames; the song plays A, B, A, B, one cycle each, the B cycles at least 15 dB under the A cycles; the bad codes are refused with `that code did not load` and exit 1; a count outside 1–256 is a usage error, refused with its range and exit 2, like a missing argument; the jazz code plays with lofi and the tool says `no kit jazz, using lofi`. The tool prints the file's duration and peak. |
| T-76 | `python3 tools/sample_generator.py spec/kits/lofi/kit.json`, then `tools/kit_builder.py` on the kit, then on a kit whose kick names a stereo, 44.1 kHz, 8-bit, over-two-second, empty, non-WAV, missing, out-of-folder, symlinked or piped file, or a name outside letters, digits, `_`, `-` and `.wav` (a space, a dot first, a CR, LF or NUL, no `.wav`), and on one whose kick is exactly two seconds; the generator on a kit with a sample pad it has no recipe for, or whose source breaks the name rule, or is an existing directory, link or pipe (§12 rule 6, D-081) | The generator writes kick, snare, hat, clap and rim as 16-bit 48 kHz mono WAVs of at most two seconds each, byte-identical on every run and matching the committed files; the builder accepts the kit, the two-second sample included, and regenerates the same header; each bad kit is refused with a one-line reason and exit 1; the generator refuses the bad pad before writing any file; the synth pads have no WAV. The Python half runs as `tests/tools_test.py`, the reader and folder half as a doctest. |
| T-77 | An impulse into the reverb (§12 sound spec, D-079) | A tail: the first 100 ms are the loudest, half a second in it is quieter but still sounding, two seconds in it is more than 60 dB down; the left and right channels differ. |
| T-78 | Press a pad while the loop plays; on the host, a press that waited 1 ms in the platform's queue (§6.7, §7.4, D-084, D-085) | The audition is rendered at the first frame of the very next audio block after the press; the app measures press-to-pickup from the input's timestamp to the audio callback's clock and prints it with the platform's output buffer (`audition latency: press read to render pickup 1.0 ms measured …`), with the main loop's worst gap since the last line, which bounds what the loop adds before it reads a press; the simulator prints one such line per press, so the 8 ms of §7.4 is read, not assumed. A release stamped a millisecond before its press, as a millisecond input clock can do, is still a tap. |
| T-79 | Every pad ×4 and split to 4 hits, chance 1, fx 1 (T-12's pattern), played for two cycles through the app on the fake HAL (§12 rule 4, D-074, D-084) | A counting global `operator new` sees zero allocations inside the audio callback and inside the scheduler's timer callback while the pattern fires. |
| T-80 | Kick ×4 in A, playing; press B at 0.30 (a copy, so the switch waits for the cycle boundary); turn the filter knob one detent down; hold the hat pad and press volume down once (§8.3, D-086 revisited 2026-09-03) | The turn is heard at once: A, still playing, has filter 0.9, and B, waiting, has 0.9 too; the hat's level is 0.7 in both; the message row shows `filter 0.9`, then `hat level 0.7`, each for a second (D-098); the master volume is untouched; when B starts nothing jumps. With no switch pending only the current section changes. |
| T-81 | Song `AAB` playing from the song view, A holding a kick; press undo three times, or hold dice (§9.6, §6.8, D-086) | Each undo removes a letter; when the last one goes, or dice clears them, song play ends at once (`song is empty`, or `song cleared`), the transport keeps running and the section that was playing plays on live from the next cycle boundary; nothing divides by zero. Play in the song view then says `song is empty` and changes nothing. Stopping a song leaves no pending song, switch or roll behind, so the next play plays the current section. |
| T-82 | Kick ×4 playing; press play to stop 5 ms before the kick at 1/4, inside the scheduler's 10 ms lookahead; then play again (§8.2, D-084) | The kick at 1/4 does not sound: every hit already handed to the audio side is dropped at the stop, and sounding voices are left to their tails; play again starts a fresh cycle whose hits all fire. |
| T-83 | Play across the audio clock's 32-bit block count: 2^32 blocks, 132 days at 48 kHz (§6.7, §7.4, D-084) | The position the scheduler and the ring read never goes backwards: it carries on past 2^32 × 128 frames one block at a time, so a hit due after the wrap lands on its sample. |
| T-84 | Kick ×15 playing; hold split past 300 ms and hold the hat pad; then the scheduler's timer stalls across a 1/16 point that a kick follows within the lookahead (§8.2, D-084, D-085) | The hat retriggers at every 1/16 of the cycle at its exact sample while the kicks keep theirs; after the stall the late roll hit sounds in the very next block, not behind the kick handed over after it, because hits reach the audio side in sample order whatever their source; releasing split ends the roll. |
| T-85 | Kick ×2; turn the filter knob one detent down; 0.9 s, 1.1 s and 1.9 s later; arm split, then show (§9.1, §8.3, Appendix D "Overlays", D-091, D-098) | The ring's corners read `100` top-left and `A 1 87%` top-right (the harness's battery); `2 kicks, spread evenly` sits in the message row and the two rows above it hold nothing but background, the kick's six-o'clock dot included; `filter 0.9` takes the message row at once, is still there at 0.9 s and gone at 1.1 s, when the status is back until 1.8 s is up; `split` sits in the top row after the bpm on the ring and at the row's right in the text view, in the accent, while armed. |
| T-86 | Kick ×3, snare ×2, hat ×1 then split, clap ×2 then swap ×2, chord ×4, bass ×2; then the hat at speed 2 and 0.5; then kick ×16; then every pad ×16 (§9.2, D-094) | The text view reads `kick   bd bd bd`, `snare  ~ sd ~ sd`, `hat    hh hh*2`, `clap   <~ cp ~ cp>` with `(alt: b)` wrapped to the next row, `bass   bass bass`, `chord  Cm Ab Eb Bb`, each name in its track colour, from y 20; the hat becomes `[hh hh*2]*2` then `[hh hh*2]/2`; sixteen kicks wrap at the 25th column with the continuation indented to the notation column; more rows than fourteen end in `...`, and than ten while the prompt rows show; an empty loop reads `nothing yet`. |
| T-87 | Kick ×1, show twice, A A B A B, play; then 59 more A and one more (§9.6, D-095) | `song 1` top-left; `empty` where the letters go until there are some, then `AABA B` in fours and the hint gone (T-92); tile 1 in the accent with its digit, tile 2 an outline; in song play the letter of the playing cycle is in the accent; the 64th letter fills the fourth row and the 65th says `song is full`. |
| T-88 | Kick ×4, snare ×2, hat ×2 (G-04); hold show; tap the rim; press show (§9.3, §10.2, D-093) | The share view: a 37-module QR (version 5) at 3 px per module inside a light square, its four-module quiet zone, below the top row; a glyph's width to its right the code in 13-column rows that break only after `:` or `-`: `RT2:lofi:100:`, `10:2:0:15:cm:`, `e10000-`, `e1.0.0-`, `e10000-e1-e1-`, `e1-e1-e1`; under the square `scan with a phone to play` in the legend colour; the QR read off the screen decodes to `https://rota.example/#` followed by G-04 (T-45); the rim tap changes the code and the QR with it; show returns to the ring. Every golden section code's QR decodes the same way; G-14 is version 10, 57 modules, its 24-character tracks fit no row beside it, and its text ends in `...`. |
| T-89 | Kick ×1; hold show then undo, and undo then show; the knobs; play on `run tutorial`; play and then a hold of play on `factory reset` (§9.4, D-096) | Settings opens either way with no undo fired; the title, thirteen rows with their values (`key c`, `scale minor`, `swing 0.15`, `kit lofi`, `brightness 100%`, `sleep 10 min`, four `on`, `firmware 0.1.0`, `run tutorial play`, `factory reset hold play`), the selected row in the accent, the footer `speed picks, filter sets` once the tap's status ends; the filter knob sets the row (`c#` then `eb` for the key, `major`, `0.20`, `80%` reaching the HAL's backlight, `5 min` then `off`, `off` and `on`), the speed knob moves the selection and wraps without touching the tempo; fx still turns fx; pads add nothing; show leaves. play on `run tutorial` starts it on the ring; play on `factory reset` says `hold play to reset` and changes nothing; a hold empties every section and the arrangement, returns to A on the ring, and the tutorial is pending. |
| T-90 | A fresh device; tap the kick; play; 50 ms and 500 ms after the first kick (§7.2, §8.2, D-099) | Every pad at 8 % of its colour; the kick at 100 % for the press, 35 % 200 ms later, the snare still at 8 %; 100 % for 100 ms after the hit at 0, 35 % again by 500 ms. |
| T-91 | Arm and disarm split and skip; hold split; show once, then twice more; play; B pressed mid-cycle; play again (§7.2, §8.2, §8.6, D-099) | split, swap, show, play and A rest at a faint glow, skip, undo and dice dark, A in the accent; split in the accent while armed and during the roll; skip in the accent while armed, dark after; show in the accent in the text view, resting on the ring; play in the accent while the transport runs; after the B press B is in the accent and A at 40 % until the boundary, then A rests; play stops and rests. |
| T-92 | Open the song view; leave and return; press B; leave and return; power off and on; open the song view and tap a pad (§9.6, D-095) | The four hint rows show, above the room the tutorial's prompt would take, and still show after leaving and returning; after B they are gone and stay gone for the power cycle; after the power cycle they show again until a pad picks a song. |
| T-93 | First boot; kick ×2; hold undo + show to open settings; turn chance; press show (§8.5, §9.4, D-096, D-097) | Settings draws no prompt, so the chance turn advances no step: the tutorial still waits for the snare when the view returns, and the same turn changes the loop's chance as it does anywhere. |
| T-94 | First boot with a card that refuses writes; press play to skip; a frame; the card accepts again; a frame (§8.5, D-097) | The skip is not lost: nothing is on the card and the flag stays pending, so the next tick writes it again; once the card takes it the flag clears and the next boot does not run the tutorial. |
| T-95 | Kick x4; hold play; four presses half a second apart; then hold play, one press and 6 s; then four presses inside one instant, and four a beat and a half apart; then, playing with B waiting, four a quarter second apart; and a hold of play on a first boot (§8.2, §8.5, D-102) | The hold opens tap tempo without starting the loop: `tap 4 times in rhythm` in the message row, `tap` in the accent in the top row after the bpm, play's backlight in the accent. The presses count down `3 more`, `2 more`, `1 more`, and none of them plays or stops; the fourth sets 120 bpm on the sections a knob would reach, says `120 bpm` and ends the mode, so a fifth press plays. The lone tap times out 5 s after itself: `tempo unchanged`, the tempo still 120. Taps inside one instant and taps at 240 bpm both clamp to 180; taps a beat and a half apart clamp to 60. On a first boot the hold skips the tutorial instead, as a press does. |
| T-96 | A: kick x4; B: that copy plus snare x2 at 105 bpm; back to A; hold A and then B together; undo; then B and undo; then hold C and D; then A, B and C together (§8.2, D-103) | `swapped A and B`: A holds what B held and B what A held, one undo level added to each, and neither release switches section, so the player stays on A. The tempo is part of a section, so it travels with the contents: A reads 105 and B 100. Undo in A brings its own kicks back at the tempo A now has, since undo never moves a knob (D-035), and undo in B its own snares. While the transport runs the swap publishes no sound parameters of its own: the beat boundary that takes the pattern publishes them with it (§6.7). C and D, neither with steps, say `nothing to swap` and record no undo. With three held one pair swaps, A and B in letter order, C is untouched, and both of the pair stay spent until they are released. |

## Watch in testing

Design bets with a known fallback. Observe them in usability round 1 (PRD §14, phase 2); the fallback is written down so nobody re-derives it.

- **A press mutes the pad's track until the release (D-085, T-35).** A tap is also a mute for the 50–150 ms the finger is down, so a pattern hit on that pad that lands inside a tap is skipped; the audition sound plays at the press and masks it. Watch whether fast repeated taps on a busy track (hats at speed 2) ever sound like a dropped hit. Fallback, not implemented: start the mute only when a press passes the 300 ms hold threshold, at the cost of a DJ kill that arrives 300 ms late.
- **Song-view pads (D-030, T-54, T-56).** In the song view the pads switch songs instead of playing sounds. Watch whether testers tap a pad expecting a sound, and whether an accidental switch to an empty song — which copies the current one — confuses them (a duplicated song that looks like a lost one). Fallback, not implemented: hold a pad to switch songs, so a plain tap stays inert in the song view.

## Open points

A new gap goes here with its scenario ID until the PRD answers it. Everything else open on 2026-09-02 was decided the same day: see D-018–D-044 in `DECISIONS.md`.

- **T-59, lineage on share (io/, session 7).** §10.2 says the device generates a 6-char id for every loop that is shared and that loading a code stores its id as the child's lineage; `spec/share-format.md` §2 and §4 say `~id` names the loop this one was loaded from and is written only when the loop was loaded from a code that carried one. The two readings differ on what the share view writes after `~`: the shared loop's own fresh id (so the receiver's lineage names its parent, and every shared code carries an id) or the lineage the loop already holds (so a re-shared loop names its grandparent, and a loop made from scratch shares with no id). The engine encodes `State::lineage` as it stands; the id generator and the replace-on-share step are io/. Decide the reading, then fill T-59's expected column and add a golden code if the canonical form changes.
- **T-62, share-code size cap (io/, session 7).** `engine/share.cpp` is linear in its input and never allocates, but nothing yet bounds what reaches it. A valid section code is at most 238 characters and a song code at most 988 (share-format §5 and §6; `kSectionCodeCapacity` and `kSongCodeCapacity` in `engine/limits.h`), so each input path caps its bytes before calling decode: the SysEx payload on the jam link (§11), the USB text import (§7.6) and the player's paste box (§9.5). Decide the cap (the code capacities plus whatever room the unknown future fields of T-16 deserve), make an over-long code fail with `that code did not load` (Appendix D), then fill T-62's expected column and add a test per path.
