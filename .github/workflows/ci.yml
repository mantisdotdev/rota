# Runs the exact commands from CLAUDE.md on every push and pull request (D-017).
# A green host build says nothing about the firmware, so both jobs run every time.
name: ci

on: [push, pull_request]

# Nothing here writes to the repository; the token only needs to read it.
permissions:
  contents: read

jobs:
  host-and-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Layer check (PRD §12 rule 1)
        # engine/ and sound/ must compile anywhere: no Arduino, Teensy, SDL or hal/ includes (D-029).
        run: |
          ! grep -rnE '#include[[:space:]]*[<"](Arduino|SDL|hal/|WProgram|core_pins|imxrt|usb_|EEPROM|SD\.h|Wire|SPI|Audio)' firmware/src/engine firmware/src/sound
      - name: Generated kit headers match spec/kits (PRD §12 rule 6)
        # Kits are data: the engine compiles a header generated from each kit.json.
        # Regenerate every kit and diff, so an edit to either side cannot hide; the
        # intent-to-add makes a header that was never committed show up too.
        run: |
          for kit in spec/kits/*/kit.json; do
            id="$(basename "$(dirname "$kit")")"
            python3 tools/kit_builder.py "$kit" "firmware/src/engine/kits/$id.h"
          done
          git add -N firmware/src/engine/kits
          git diff --exit-code -- firmware/src/engine/kits
      - name: Python tools (T-76)
        # The sample generator and the kit builder's WAV checks, tested where they live.
        run: python3 -m unittest discover -s tests -p 'tools_test.py' -v
      - name: Install SDL2
        run: |
          sudo apt-get update
          sudo apt-get install -y libsdl2-dev
      - name: Configure host
        run: |
          mkdir -p out/screens-raw
          cmake -S host -B build
      - name: Build host (simulator, tests, tools)
        run: cmake --build build
      - name: Test all
        run: ROTA_SCREENS=out/screens-raw ./build/tests
      - name: Reference renders match what the views draw (D-101)
        # The tests write the screen at each state they check; this turns those into
        # the PNGs the repo keeps and diffs them, so a view change cannot leave the
        # documentation of it behind. Same shape as the kit headers above.
        run: |
          python3 tools/screens.py out/screens-raw spec/screens
          git add -N spec/screens
          git diff --exit-code -- spec/screens
      # Never run ./build/simulator here: it opens an SDL window.

  firmware:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio-${{ hashFiles('firmware/platformio.ini') }}
          restore-keys: ${{ runner.os }}-pio-
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - name: Install PlatformIO
        run: pip install --upgrade platformio
      - name: Build firmware
        run: pio run -d firmware -e teensy41
